# 2026-09 update log

Index and query commands: [README.md](README.md). New entries go at the end.

---

## U-20260922-01 · 2026-09-22 · Adopt progress/architecture/docs-updates rules · #docs #migration

- **What**: `CLAUDE.md` gained the workspace rule section (commit at every stage; `progress.md` holds open items only; finished work is recorded in `docs/updates/`; `architecture.md` is the short overview). Added `architecture.md` and this `docs/updates/` index. `PROGRESS.md` is now tracked (removed from `.gitignore`); its PyBreeze item moved to PyBreeze's `progress.md`, and the open items from the workspace inventory of 2026-09-22 were added.
- **Files**: `CLAUDE.md`, `.gitignore`, `PROGRESS.md`, `architecture.md`, `docs/updates/`.
- **Open items**: see `progress.md`.

## U-20260922-02 · 2026-09-22 · Stop tracking .idea/ · #done #housekeeping

- **What**: removed the JetBrains settings under `.idea/` from version control with `git rm -r --cached .idea` (1 files); the files stay on disk. Workspace item W-4 (`D:\Codes\progress.md`).
- **Files**: `.idea/` (untracked); `.gitignore` already ignored `.idea/`.
- **Open items**: none.

## U-20260922-03 · 2026-09-22 · Point project URLs at the current repository · #done #metadata

- **What**: replaced the misspelled or renamed GitHub owner in the project URLs (`Intergration-Automation-Testing/…`, and where present `JE-Chen/je_editor`, `JE-Chen/Integration-testing-environment`, the `AutoControl`/`AutomationEditor`/`AutomationFile`/`JEditor` repo names) with the current `origin`. `JE-Chen/je_editor` becomes `Integration-Automation/JEDITOR` in the package metadata, README badges and clone commands, and the getting-started docs. Workspace item X-11.
- **Files**: `README.md`, `README/README_zh-CN.md`, `README/README_zh-TW.md`, `dev.toml`, `docs/source/docs/Eng/getting_started.rst`, `docs/source/docs/Zh/getting_started.rst`, `pyproject.toml`.
- **Open items**: none.

## U-20260922-04 · 2026-09-22 · PLUGIN_GUIDE.md becomes the single plugin guide · #done #docs

- **What**: `PLUGIN_GUIDE.md` pointed at a nonexistent `exe/jeditor_plugins/` for the example plugins. It now names the IDE_Plugins repository (`program_languages/…`, `languages/french.py`), gains an *Installing Plugins* section (Plugin Browser → `jeditor_plugins/` under the working directory, restart; or copy by hand), and documents what `plugin_loader.py` actually does: both scan roots (working directory, then next to the `je_editor` package), category directories without `__init__.py`, first-found-wins, and the warning for a module without `register()`. The title and intro now say it is the one guide for JEditor and PyBreeze; PyBreeze's copy becomes a pointer (workspace X-10). The Sphinx page `docs/source/docs/{Eng,Zh}/plugins.rst` got the same corrections. Closes `PROGRESS.md` #7.
- **Checks**: `sphinx-build -b html docs/source` gives no warning for `plugins.rst`.
- **Files**: `PLUGIN_GUIDE.md`, `docs/source/docs/Eng/plugins.rst`, `docs/source/docs/Zh/plugins.rst`, `PROGRESS.md`.
- **Open items**: none.

## U-20260923-01 · 2026-09-23 · JEditor.log moves out of the working directory · #done #logging

- **What** (workspace item X-6): `je_editor/utils/logging/loggin_instance.py` opened the relative path `JEditor.log` in overwrite mode at import, so every process that imported the package (PyBreeze, test runs) left a `JEditor.log` in its working directory and wiped the one the previous process wrote; `D:\Codes` and several sibling repositories had collected them. The handler also had no `encoding`, so on a cp950 console any record with a character outside cp950 raised inside `emit()` and was dropped.
- **Now**, following AutoControlGUI's `5714a59`: the file is `$JE_EDITOR_LOG_FILE` or `~/.je_editor/logs/JEditor.log`; the package handler is built with `delay=True`, so importing writes nothing and the directory is created on the first record; append mode with the process id on each line, because every process on the account shares the file; rotation to `.1` past 10 MiB only when a process opens the file (Windows cannot rename a file another process holds); UTF-8 with `errors="backslashreplace"`; an unopenable path falls back to `os.devnull` with one `RuntimeWarning` instead of failing the import. `JEditorLoggingHandler` keeps its keyword arguments and gains `encoding`, `errors`, `delay`; `mode` now defaults to `"a"` and `filename` to `default_log_file()`.
- **Tests**: `test/test_log_location.py` (9): home default, environment override, loading the module in a fresh interpreter leaves the cwd empty and creates no directory (the old module leaves `JEditor.log` there), first record creates the directory and later handlers append, text outside cp950 and a lone surrogate survive, an unopenable path only warns, the package handler's path, mode and encoding. Full suite (`--ignore=test/qt_ui`) 1875 passed; `ruff check .` clean.
- **Docs**: `docs/source/docs/{Eng,Zh}/api_reference.rst` (where the log goes), `architecture_explore.md` (module row; package sizes re-measured: `pyside_ui/` 20,330, `utils/` 8,668, total 30,608 lines).
- **Files**: `je_editor/utils/logging/loggin_instance.py`, `test/test_log_location.py`, `docs/source/docs/Eng/api_reference.rst`, `docs/source/docs/Zh/api_reference.rst`, `architecture_explore.md`.
- **Open items**: none here; FrontEngine and WebRunner remain under workspace X-6.

## U-20260923-02 · 2026-09-23 · langchain_openai 1.6.2; Dependabot targets dev · #done #deps #ci

- **What** (workspace item X-16, part of `PROGRESS.md` #4 and #6): `langchain_openai` moves from `==1.4.1` to `==1.6.2` in `pyproject.toml` and `requirements.txt`, and `dev.toml` — which was three releases further behind at `==1.2.0` — is brought to the same pin. Dependabot PR #262 is closed with a pointer to this commit.
- **Dependabot** now sets `target-branch: "dev"` in `.github/dependabot.yml`. Its PRs opened against `main`, the released branch, which is why #262 and #260 sat unmerged.
- **Checks**: installed 1.6.2 into the local `.venv`; `from langchain_openai import ChatOpenAI` (the only symbol `ai_widget/langchain_interface.py` imports) still resolves. Full suite `--ignore=test/qt_ui`: 1875 passed; `ruff check .` clean.
- **Files**: `pyproject.toml`, `requirements.txt`, `dev.toml`, `.github/dependabot.yml`.
- **Open items**: `PROGRESS.md` #4 still covers the stale `dev.toml` version and the PySide6 line.

## U-20260923-03 · 2026-09-23 · PySide6 6.11.2 across the three pin files · #done #deps

- **What** (workspace item X-1, and Dependabot PR #260): PySide6 moves to `==6.11.2` in `pyproject.toml`, `requirements.txt` and `dev.toml` (which lagged at 6.11.0), and `CLAUDE.md`'s Project Overview line is corrected — it still said 6.11.0 while the package pinned 6.11.1. 6.11.2 carries the `QAction.menu()` wrapper-invalidation fix other workspace projects are waiting for.
- **Order matters here**: the published `je_editor` and `frontengine` pin PySide6 exactly, and PyBreeze pins the same version, so the three move one after another, not at once. JEditor goes first and releases; FrontEngine follows; PyBreeze bumps once the published `je_editor` carries 6.11.2 (its `architecture.md` §6 records that rule).
- **Checks** with 6.11.2 installed locally: `pytest --ignore=test/qt_ui test` 1875 passed; the two CI Qt scripts `test/qt_ui/unit_test/start_qt_ui.py` and `extend_test.py` both exit 0 under `QT_QPA_PLATFORM=offscreen`; `ruff check .` clean.
- **Files**: `pyproject.toml`, `requirements.txt`, `dev.toml`, `CLAUDE.md`.
- **Open items**: none here.
